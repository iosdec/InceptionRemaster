# =============================================================================
#  COPYRIGHTED CONTENT — NEVER COMMIT
# =============================================================================
# None of the original app's media is ours to redistribute. It is extracted at
# runtime from the user's own legally-owned copy of the app (see README).
# This project ships ZERO bytes of it. These rules are belt-and-suspenders.

# Scene bundles — RjDj Pure Data sources + Warner Bros / Hans Zimmer audio
*.rjz
*.rj/
RJDJScenes/
**/RJDJScenes/

# Extracted UI artwork (tiles, chrome, film key art) and interaction sounds
**/Assets.xcassets/DreamPieces/
**/Assets.xcassets/Chrome/
**/Resources/Sounds/
*.m4a
*.wav
*.aif
*.aiff
*.caf

# Any user-supplied original app package
*.ipa

# =============================================================================
#  Local identity / signing — never commit
# =============================================================================
# Personal Team ID, bundle identifier and signing live only on your machine.
*.xcconfig
!*.xcconfig.example
Local.xcconfig

# The Xcode project is generated by Tuist — do not commit it.
*.xcodeproj
*.xcworkspace

# Tuist
Tuist/.build/
.tuist-cache/
Derived/
graph.png

# =============================================================================
#  Xcode / Swift / macOS
# =============================================================================
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout
DerivedData/
build/
*.moved-aside
*.hmap
*.dSYM.zip
*.dSYM
.build/
.swiftpm/

.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Local copies of copyrighted source assets — kept for re-import, never built or committed
_LocalAssets/
